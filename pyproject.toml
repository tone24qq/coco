[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rf_infer"
version = "0.1.0"
dependencies = [
    "scikit-learn>=1.2.0",
    "joblib",
    "numpy",
]

[project.scripts]
rf-infer = "rf_infer.cli:main"
