# === Formatting / Utilities ===
black==25.1.0
isort==6.0.1
httpx==0.28.1        # HTTP client, FastAPI 測試常用

# === Lint / Type / Security ===
flake8==7.0.0
mypy==1.10.0
bandit==1.7.7
pip-audit==2.9.0

# === Core runtime (數值 / 推論) ===
numpy>=1.26
scipy>=1.16
pydantic==2.11.0
orjson==3.10.1
torch==2.3.0         # ← 若服務端沒用到 PyTorch，可移除此行

# === Testing ===
pytest==8.2.2
hypothesis==6.103.0
pytest-xdist==3.5.0
pytest-cov==5.0.0

# === Optional / Misc ===
openpyxl==3.1.2      # Excel 讀寫，如無需求可刪
ortools==9.9.3963    # Google OR-Tools，僅演算法研究才需要