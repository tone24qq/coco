data:
  path: samples/data/full_boards_10x8.json

modules:
  focus: true
  connectivity: true
  heatmap: true
  a2: true
  d3: true
  skip: true
  diff: true
  mirror: true
  tail: true
  f10: true

eval:
  folds: 3
  masking_repeats: 5
  seed: 2026

search:
  n_trials: 8

reports:
  summary_report: reports/masking_summary_report.json
  per_target_predictions: reports/masking_per_target_predictions.csv
  best_config: reports/masking_best_config.json
  data_audit: reports/masking_data_audit.json


discovery:
  repeats: 1
  n_trials: 2
  candidate_modules: ["local_arith_completion", "local_delta_consistency", "modulo_family", "a2_delta"]
